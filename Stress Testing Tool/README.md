# Installing Stress Testing tool on Linux
## To install Stress Testing tool on Linux, follow the steps below:

To update the system, use the command
```
sudo apt update && sudo apt upgrade -y
```

To install Stress Testing tool, use the command
```
sudo apt install stress-ng -y
```

To check the version, use the command
```
stress-ng --version
```